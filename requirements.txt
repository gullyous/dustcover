# GUI toolkit
PySide6>=6.6.0

# Windows Runtime projection - gives Python access to the System Media
# Transport Controls (SMTC). Single package, bundles all namespaces.
# If this ever fails to install, the code also accepts the newer split
# "winrt-Windows.*" packages (see media_backend.py import shim).
winsdk>=1.0.0b10

# Optional: TIDAL "favorite / add to my collection" support for the heart
# button (unofficial TIDAL API client). The widget runs fine without it;
# the heart just reports that signing in is needed.
tidalapi>=0.8.0

# Optional: system-wide global hotkeys. The widget runs fine without it
# (hotkeys are simply unavailable).
pynput>=1.7.6

# Optional: per-app volume control via the Windows Core Audio APIs. Pulls in
# comtypes + psutil. Without it, the volume slider is simply hidden.
pycaw>=20240210

# Optional: Discord Rich Presence ("Listening to TIDAL"). Pure Python, no deps.
# Without it, that feature is simply unavailable. (ListenBrainz scrobbling and
# the OBS overlay need no extra packages; they use the standard library / Qt.)
pypresence>=4.3.0
